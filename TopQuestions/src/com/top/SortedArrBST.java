package com.top;

public class SortedArrBST {

/*	public static void main(String[] args) {
		int[] arr = new int[]{1,2,3,4,5,6,7,8};
		makeBSTFromArray(arr, 0, arr.length);
	}
	
	private static TNode makeBSTFromArray(int[] arr, int min, int max){
		if(min>max){return null;}
		
		int middle=(min+max)/2;
		TNode node = createNode(arr[middle]);
		node.left = makeBSTFromArray(arr, min, middle-1);
		node.right = makeBSTFromArray(arr, middle+1, max);
	} */

}
